{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the main and tray windows",
  "windows": ["main", "tray"],
  "permissions": ["core:default"]
}
